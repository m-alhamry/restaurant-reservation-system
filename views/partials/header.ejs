<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Reservation System</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <header>
        <nav>
            <% if (typeof user !=='undefined' && user) { %>
                <a href="/users/profile" class="<%= currentPath === '/users/profile' ? 'active' : '' %>">My Profile</a>
                <% if (user.role==='customer' ) { %>
                    <a href="/reservations" class="<%= currentPath === '/reservations' ? 'active' : '' %>">My
                        Reservations</a>
                <% } else if (user.role==='staff' ) { %>
                    <a href="/staff/reservations"
                        class="<%= currentPath === '/staff/reservations' ? 'active' : '' %>">All Reservations</a>
                    <a href="/staff/timeSlots"
                        class="<%= currentPath === '/staff/timeSlots' ? 'active' : '' %>">Manage Time Slots</a>
                <% } %>
                <a href="/auth/sign-Out"
                    class="<%= currentPath === '/auth/sign-Out' ? 'active' : '' %>">Sign-Out</a>
            <% } else { %>
                <a href="/auth/sign-in"
                    class="<%= currentPath === '/auth/sign-in' ? 'active' : '' %>">Sign-In</a>
                <a href="/auth/sign-up"
                    class="<%= currentPath === '/auth/sign-up' ? 'active' : '' %>">Sign-Up</a>
            <% } %>
        </nav>
    </header>
    <main>